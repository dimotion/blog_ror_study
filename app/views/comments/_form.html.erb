<h3>Add a comment</h3>

<%= form_for([commentable, Comment.new]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title%>
  </p>

  <p>
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </p>

  <p>
    <%= f.submit "Create"%>
  </p>

<% end %>